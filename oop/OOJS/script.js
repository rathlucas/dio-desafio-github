// REVISAR CONTEÚDO EM OUTRO CURSO! DIDÁTICA PORCA!


/* class Animal {
    constructor(type = "animal") {
        this.type = type;
    }

    get type() {
        return this._type;
    }

    set type(val) {
        this._type = val;
    }

    makeSound() {
        console.log("Making animal sounds...");
    }
}

class Dog extends Animal {
    constuctor(type = "dog") {

        this.type = "dog";
    }

    makeSound() {
        super.makeSound();
        console.log("Bark Bark!")
    }
}

let a = new Animal();
console.log(a.type);

let b = new Dog();
console.log(b.type); */

